{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user within the EduSpark application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user."
        },
        "username": {
          "type": "string",
          "description": "The username of the user."
        },
        "email": {
          "type": "string",
          "description": "The email address of the user.",
          "format": "email"
        },
        "points": {
          "type": "number",
          "description": "Total points earned by the user."
        },
        "badgeIds": {
          "type": "array",
          "description": "References to Badges earned. (Relationship: User 1:N Badge)",
          "items": {
            "type": "string"
          }
        },
        "studySessionIds": {
          "type": "array",
          "description": "References to StudySession. (Relationship: User 1:N StudySession)",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "id",
        "username",
        "email"
      ]
    },
    "QuizResult": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "QuizResult",
      "type": "object",
      "description": "Stores the results of a quiz taken by a user, including personalized feedback and suggestions.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the quiz result."
        },
        "userId": {
          "type": "string",
          "description": "Reference to User. (Relationship: User 1:N QuizResult)"
        },
        "quizId": {
          "type": "string",
          "description": "Reference to Quiz. (Relationship: Quiz 1:N QuizResult)"
        },
        "totalMarks": {
          "type": "number",
          "description": "Total marks obtained in the quiz."
        },
        "percentage": {
          "type": "number",
          "description": "Percentage score achieved in the quiz."
        },
        "feedback": {
          "type": "array",
          "description": "Personalized feedback for each incorrect answer.",
          "items": {
            "type": "string"
          }
        },
        "suggestions": {
          "type": "array",
          "description": "Specific improvement suggestions based on wrong answers.",
          "items": {
            "type": "string"
          }
        },
        "pointsEarned": {
          "type": "number",
          "description": "Points earned by the user for this quiz."
        },
        "submissionDateTime": {
          "type": "string",
          "description": "The date and time when the quiz was submitted.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userId",
        "quizId",
        "totalMarks",
        "percentage",
        "pointsEarned",
        "submissionDateTime"
      ]
    },
    "Quiz": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Quiz",
      "type": "object",
      "description": "Represents a quiz, containing questions and metadata.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the quiz."
        },
        "name": {
          "type": "string",
          "description": "The name or title of the quiz."
        },
        "topic": {
          "type": "string",
          "description": "The topic covered by the quiz."
        },
        "questionIds": {
          "type": "array",
          "description": "References to questions in the quiz. (Relationship: Quiz 1:N Question)",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "id",
        "name",
        "topic"
      ]
    },
    "Question": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Question",
      "type": "object",
      "description": "Represents a question in a quiz.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the question."
        },
        "quizId": {
          "type": "string",
          "description": "Reference to Quiz. (Relationship: Quiz 1:N Question)"
        },
        "text": {
          "type": "string",
          "description": "The text of the question."
        },
        "correctAnswer": {
          "type": "string",
          "description": "The correct answer to the question."
        },
        "options": {
          "type": "array",
          "description": "The options for the question.",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "id",
        "quizId",
        "text",
        "correctAnswer",
        "options"
      ]
    },
    "Badge": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Badge",
      "type": "object",
      "description": "Represents a badge that a user can earn.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the badge."
        },
        "name": {
          "type": "string",
          "description": "Name of the badge."
        },
        "description": {
          "type": "string",
          "description": "Description of the badge and how to earn it."
        },
        "imageUrl": {
          "type": "string",
          "description": "URL of the badge image.",
          "format": "uri"
        }
      },
      "required": [
        "id",
        "name",
        "description",
        "imageUrl"
      ]
    },
    "StudySession": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "StudySession",
      "type": "object",
      "description": "Represents a study session with friends.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the study session."
        },
        "userIds": {
          "type": "array",
          "description": "References to Users participating in the study session. (Relationship: User N:N StudySession)",
          "items": {
            "type": "string"
          }
        },
        "startTime": {
          "type": "string",
          "description": "The date and time when the study session started.",
          "format": "date-time"
        },
        "endTime": {
          "type": "string",
          "description": "The date and time when the study session ended.",
          "format": "date-time"
        },
        "topic": {
          "type": "string",
          "description": "The topic of the study session."
        }
      },
      "required": [
        "id",
        "startTime",
        "endTime",
        "topic"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores user profiles. This path is used for user-related information and settings.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier for the user."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/quizResults/{quizResultId}",
        "definition": {
          "entityName": "QuizResult",
          "schema": {
            "$ref": "#/backend/entities/QuizResult"
          },
          "description": "Stores quiz results for each user, including marks, feedback, suggestions, and points earned. This structure facilitates easy retrieval of a user's quiz history and enables personalized learning experiences.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier for the user."
            },
            {
              "name": "quizResultId",
              "description": "The unique identifier for the quiz result."
            }
          ]
        }
      },
      {
        "path": "/quizzes/{quizId}",
        "definition": {
          "entityName": "Quiz",
          "schema": {
            "$ref": "#/backend/entities/Quiz"
          },
          "description": "Stores quiz definitions, including questions and metadata.",
          "params": [
            {
              "name": "quizId",
              "description": "The unique identifier for the quiz."
            }
          ]
        }
      },
      {
        "path": "/questions/{questionId}",
        "definition": {
          "entityName": "Question",
          "schema": {
            "$ref": "#/backend/entities/Question"
          },
          "description": "Stores individual question details, associated with a specific quiz.",
          "params": [
            {
              "name": "questionId",
              "description": "The unique identifier for the question."
            }
          ]
        }
      },
      {
        "path": "/badges/{badgeId}",
        "definition": {
          "entityName": "Badge",
          "schema": {
            "$ref": "#/backend/entities/Badge"
          },
          "description": "Stores badge information, including name, description, and image URL.",
          "params": [
            {
              "name": "badgeId",
              "description": "The unique identifier for the badge."
            }
          ]
        }
      },
      {
        "path": "/studySessions/{studySessionId}",
        "definition": {
          "entityName": "StudySession",
          "schema": {
            "$ref": "#/backend/entities/StudySession"
          },
          "description": "Stores study session details, including participants, start time, end time, and topic.",
          "params": [
            {
              "name": "studySessionId",
              "description": "The unique identifier for the study session."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore structure is designed to support the quiz system enhancements, focusing on storing quiz results with personalized feedback, suggestions, and points. A path-based ownership model is used for `quizResults` under each user, ensuring data privacy and security. This structure ensures authorization independence by avoiding `get()` calls in security rules. Each `quizResult` is stored within the `users/{userId}/quizResults/{quizResultId}` path, making it simple to secure quiz results based on user ID. Segregation is achieved by storing all quiz results within the user-specific subcollection, enforcing a homogeneous security posture for all documents within that collection. The QAPs are supported by the user-based subcollection, which prevents listing other user's quiz results."
  }
}